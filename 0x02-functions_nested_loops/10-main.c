#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int result;

    result = add(5, 10);
    printf("Result: %d\n", result);

    return (0);
}

