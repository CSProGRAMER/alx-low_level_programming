#include "main.h"

int main(void)
{
	    char *str = "Reverse Me!";

	        _print_rev_recursion(str);
		    _putchar('\n'); /* Print a newline after the reversed string */

		        return (0);
}

